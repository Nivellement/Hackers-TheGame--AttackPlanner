package game.subnode;

import game.Node;

public class CodeGate extends Node {
    public CodeGate(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}