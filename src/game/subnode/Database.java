package game.subnode;

import game.Node;

public class Database extends Node {
    public Database(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}