package game.subnode;

import game.Node;

public class ProgramLibrary extends Node {
    public ProgramLibrary(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}