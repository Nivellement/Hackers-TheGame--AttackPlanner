package game.subnode;

import game.Node;

public class NetConnection extends Node {
    public NetConnection(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}