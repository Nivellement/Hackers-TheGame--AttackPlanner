package game.subnode;

import game.Node;

public class ServerFarm extends Node {
    public ServerFarm(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}