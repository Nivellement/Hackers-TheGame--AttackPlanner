package game.subnode;

import game.Node;

public class Evolver extends Node {
    public Evolver(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}