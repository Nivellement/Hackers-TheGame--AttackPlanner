package game.subnode;

import game.Node;

public class BCoinMine extends Node {
    public BCoinMine(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}