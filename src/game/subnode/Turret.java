package game.subnode;

import game.Node;

public class Turret extends Node {
    public Turret(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}