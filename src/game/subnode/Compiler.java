package game.subnode;

import game.Node;

public class Compiler extends Node {
    public Compiler(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}