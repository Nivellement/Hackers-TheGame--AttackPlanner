package game.subnode;

import game.Node;

public class BCoinMixer extends Node {
    public BCoinMixer(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}