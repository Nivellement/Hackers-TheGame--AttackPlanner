package game.subnode;

import game.Node;

public class Sentry extends Node {
    public Sentry(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}