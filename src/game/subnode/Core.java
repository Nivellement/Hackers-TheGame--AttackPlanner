package game.subnode;

import game.Node;

public class Core extends Node {
    public Core(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}