package game.subnode;

import game.Node;

public class Scanner extends Node {
    public Scanner(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}