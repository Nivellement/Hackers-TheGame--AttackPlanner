package game.subnode;

import game.Node;

public class Guardian extends Node {
    public Guardian(String id, int level, int firewall, int maxConnections) {
        super(id, level, firewall, maxConnections);
    }
}